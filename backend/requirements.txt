# FastAPI and web server
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Environment
python-dotenv>=1.0.0

# AI/LLM
openai>=1.0.0

# ========================
# GPU / CUDA Support
# ========================
# For NVIDIA GPU video processing

# PyTorch with CUDA support (uncomment if GPU available)
# torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu118

# TensorRT for GPU inference (optional)
# tensorrt>=8.6.0

# NVIDIA CUDA toolkit (install separately on host)
# nvidia-cuda-runtime-cu12

# Video processing with GPU acceleration
# accelerate>=0.20.0

# For CPU-only fallback:
moviepy>=1.0.3
opencv-python>=4.8.0

# Image/Video generation
# diffusers>=0.21.0
# transformers>=4.30.0

# Utilities
tqdm>=4.65.0
numpy>=1.24.0
